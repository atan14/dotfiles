[user]
	email = tanaikrui@hotmail.com
	name = Aik Rui Tan
[http]
	sslBackend = openssl
[alias]
	st = status -sb
	last = log -1 HEAD --stat
	cm = commit -m
	rv = remote -v
	diffvim = difftool -t vimdiff -y
	update = add --update

# Allow local customizations in the .gitconfig_local file
[include]
	path = ~/.gitconfig_local
